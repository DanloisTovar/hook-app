export const tareasReducer = (initialState = [], accion) => {
    switch (accion.type) {
        case 'algo':
            return initialState;
            break;

        default:
            return initialState;
    }
    return state;
};
